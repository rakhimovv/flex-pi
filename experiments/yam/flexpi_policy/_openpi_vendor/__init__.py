"""Vendored subset of openpi-client + openpi.serving.

Source: openpi (https://github.com/Physical-Intelligence/openpi)
        packages/openpi-client/src/openpi_client/
        src/openpi/serving/websocket_policy_server.py

Vendored verbatim except for cross-module imports rewritten to relative
(`from . import X`). Kept minimal — only modules needed for the FlexPi YAM
2-terminal websocket deploy. See docs/YAM.md for context.
"""
